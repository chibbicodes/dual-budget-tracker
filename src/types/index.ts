// Re-export all types from the shared package.
// Desktop app imports continue to work unchanged; the actual
// definitions live in packages/shared/src/types/index.ts.
export * from '@dual-budget/shared'
